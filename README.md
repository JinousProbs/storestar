# storestar

A minimal demo for searching the iTunes Apple Store API.

## Usage

1. Open `index.html` in a browser.
2. Enter a keyword into the search field.
3. Press `Search` to retrieve matching results from the iTunes search API.
